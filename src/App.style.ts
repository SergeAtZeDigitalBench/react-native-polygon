import { StyleSheet, Text, View } from "react-native";

export const s = StyleSheet.create({
  container: {
    flex: 1,
  },
  title: {
    fontSize: 30,
    paddingVertical: 10,
    textAlign: "center",
  },
});
